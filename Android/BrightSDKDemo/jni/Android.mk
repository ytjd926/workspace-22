LOCAL_PATH := $(call my-dir)

include $(CLEAR_VARS)

LOCAL_MODULE    := BRTBeaconManagerListBeaconsActivity
LOCAL_SRC_FILES := BRTBeaconManagerListBeaconsActivity.cpp

include $(BUILD_SHARED_LIBRARY)
